{
  "name": "storecops-growth-platform",
  "version": "1.0.0",
  "description": "AI-driven e-commerce growth platform: customer intelligence, competitive monitoring, market trends and automated revenue generation in one unified 6-layer system.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "pretest": "node scripts/check-syntax.js",
    "test": "node --test \"test/**/*.test.js\"",
    "lint": "eslint src/ test/ --ext .js",
    "lint:fix": "eslint src/ test/ --ext .js --fix",
    "lint:syntax": "node scripts/check-syntax.js",
    "preflight": "node scripts/preflight.js",
    "preflight:strict": "node scripts/preflight.js --strict",
    "backup": "node scripts/backup.js",
    "backup:verify": "node scripts/restore-check.js",
    "format": "prettier --write \"src/**/*.js\" \"test/**/*.js\""
  },
  "keywords": [
    "ecommerce",
    "growth",
    "recovery",
    "intelligence",
    "saas"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^4.21.2",
    "ioredis": "^5.3.0",
    "pdfkit": "^0.19.1",
    "razorpay": "^2.9.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^17.11.0",
    "prettier": "^3.2.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
