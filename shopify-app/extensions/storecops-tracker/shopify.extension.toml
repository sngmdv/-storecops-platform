# Storecops Tracker - theme app extension manifest.
#
# NOTE: theme app extension manifests are TOML, not JSON. This file
# previously contained a JSON object, which `shopify app deploy` cannot
# parse.
#
# Block settings do NOT belong here. They are declared in the
# {% schema %} block inside each .liquid file under blocks/, which is
# what the Shopify Theme Editor reads.

name = "Storecops Tracker"
type = "theme"
